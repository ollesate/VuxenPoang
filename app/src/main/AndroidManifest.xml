<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sjoholm.olof.vuxenpoang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.balance.BalanceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.sale.SellActivity" />

        <activity android:name=".ui.sale.SalesItemsActivity" />

        <activity android:name=".ui.purchase.BuyActivity" />

        <activity android:name=".ui.purchase.PurchaseItemsActivity" />

    </application>

</manifest>